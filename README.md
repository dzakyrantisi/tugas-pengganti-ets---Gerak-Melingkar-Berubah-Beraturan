# tugas-pengganti-ets---Gerak-Melingkar-Berubah-Beraturan

Repositori kini menyertakan dua varian proyek:

- `main.html` + `main.js` di akar tetap memakai implementasi WebGL asli (shader manual).
- Folder `threejs-version/` memuat adaptasi yang sama namun dibangun dengan Three.js (`threejs-version/main.html`).

Jalankan salah satu versi dengan membuka berkas HTML-nya secara langsung di peramban.